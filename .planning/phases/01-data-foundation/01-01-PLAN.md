---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - notebooks/00_download.ipynb
  - notebooks/data/raw/
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - "9 source CSV/XLS files exist in notebooks/data/raw/ with non-zero size"
    - "La Poste cp->commune correspondence table downloaded"
    - "RPPS file is readable (correct encoding detected)"
    - "Row count sanity checks pass for all major files"
  artifacts:
    - path: "notebooks/00_download.ipynb"
      provides: "Download pipeline for all 9 datasets + La Poste table"
      min_lines: 100
    - path: "notebooks/data/raw/"
      provides: "All raw data files"
  key_links:
    - from: "notebooks/00_download.ipynb"
      to: "notebooks/data/raw/"
      via: "urllib/requests download + write"
      pattern: "urlretrieve|requests.get|download"
---

<objective>
Download all 9 source datasets and the La Poste correspondence table into notebooks/data/raw/, with inline assertions validating file integrity (exists, non-empty, expected row counts, encoding).

Purpose: All raw data available locally for the merge step.
Output: notebooks/00_download.ipynb + 10 files in notebooks/data/raw/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-VALIDATION.md
@datasets-santescope.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 00_download.ipynb — download all 9 datasets + La Poste table</name>
  <files>notebooks/00_download.ipynb</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-CONTEXT.md
    - .planning/phases/01-data-foundation/01-VALIDATION.md
    - datasets-santescope.md
  </read_first>
  <action>
Create a Jupyter notebook `notebooks/00_download.ipynb` that downloads all 9 datasets + the La Poste cp->commune table into `notebooks/data/raw/`.

The notebook must have these sections (one cell group per dataset):

**Cell 0 — Setup:**
```python
import os, urllib.request, zipfile, io
import pandas as pd
RAW = "data/raw"
os.makedirs(RAW, exist_ok=True)
```

**Cell 1 — APL (DREES):**
- URL: Go to https://www.data.gouv.fr/datasets/laccessibilite-potentielle-localisee-apl/ and find the CSV resource URL for the latest year (2023). The file is typically named something like `apl_medgen_com_YYYY.csv`.
- Save as: `data/raw/apl.csv`
- Assert: file exists, >30K rows, has columns containing "commune" and "apl"

**Cell 2 — RPPS (Annuaire Sante):**
- URL: https://www.data.gouv.fr/datasets/annuaire-sante-extractions-des-donnees-en-libre-acces-des-professionnels-intervenant-dans-le-systeme-de-sante-rpps — find the main CSV resource (PS_LibreAcces, ~761 Mo)
- This is a large file. Download with `urllib.request.urlretrieve` (not requests, to avoid memory issues).
- Save as: `data/raw/rpps.csv`
- Try reading with UTF-8 first, fallback to Latin-1: `pd.read_csv(..., encoding='utf-8', nrows=5)` wrapped in try/except
- Assert: file size > 100MB, readable, >1M rows (check with `sum(1 for _ in open(...))` or `pd.read_csv(..., usecols=[0]).shape[0]`)

**Cell 3 — FINESS:**
- URL: https://www.data.gouv.fr/datasets/finess-extraction-du-fichier-des-etablissements — find the CSV with establishment data
- Save as: `data/raw/finess.csv`
- Assert: >50K rows

**Cell 4 — MSP:**
- URL: https://www.data.gouv.fr/datasets/maisons-de-sante-pluriprofessionnelles — find CSV
- Save as: `data/raw/msp.csv`
- Assert: >1K rows (there are ~2500 MSPs in France)

**Cell 5 — INSEE Population:**
- URL: https://www.insee.fr/fr/statistiques/8680726?sommaire=8681011 — find the commune-level CSV (population municipale by commune)
- Save as: `data/raw/insee_pop.csv`
- Assert: >34K rows, has column containing "DEPCOM" or "COM"

**Cell 6 — FiLoSoFi (revenus/pauvrete):**
- URL: https://www.data.gouv.fr/datasets/revenus-et-pauvrete-des-menages-aux-niveaux-national-et-local-revenus-localises-sociaux-et-fiscaux — find commune-level CSV for 2021
- Save as: `data/raw/filosofi.csv`
- Assert: >30K rows, has column with "pauvrete" or "TPMP" or "TP60"

**Cell 7 — Pathologies (data.ameli):**
- URL: https://data.ameli.fr/explore/dataset/effectifs/ — export as CSV (look for download/export link)
- Save as: `data/raw/pathologies.csv`
- Assert: >50 rows (department-level, ~100 departments x pathology types)

**Cell 8 — Admin Express (GeoJSON):**
- URL: https://www.data.gouv.fr/datasets/admin-express-1 — find the GeoJSON for communes (or download the full package and extract communes geojson)
- Save as: `data/raw/communes_geo.geojson` (or .json)
- Assert: file exists, >1MB

**Cell 9 — Diagnostic acces soins urgents:**
- URL: https://www.data.gouv.fr/datasets/diagnostic-dacces-aux-soins-urgents — find XLS/XLSX
- Save as: `data/raw/urgences.xlsx`
- Convert to read with `pd.read_excel()`
- Assert: >30K rows, has column related to temps/minutes

**Cell 10 — La Poste correspondence table (cp -> commune):**
- URL: https://www.data.gouv.fr/datasets/base-officielle-des-codes-postaux/ — find CSV
- Save as: `data/raw/la_poste_cp_commune.csv`
- Assert: >30K rows, has code_postal and code_commune columns (or similar names like Code_postal, Code_commune_INSEE)

**Cell 11 — Summary:**
Print a recap table: dataset name, filename, file size, row count (where applicable), status (OK/FAIL).

**IMPORTANT NOTES:**
- data.gouv.fr URLs often redirect. Use the actual resource download URL, not the dataset page URL. You may need to browse the dataset page to find the direct CSV link.
- For each download, wrap in try/except with a clear error message.
- If a URL gives a ZIP, extract the relevant CSV from it.
- Use `urllib.request.urlretrieve` for large files (RPPS, Admin Express).
- Print progress: "Downloading {name}..." before each download.
- The notebook should be re-runnable (skip download if file already exists and has expected size — add a simple `if os.path.exists(path) and os.path.getsize(path) > min_size: print("Already downloaded"); skip`).
  </action>
  <verify>
    <automated>cd notebooks && jupyter nbconvert --to script 00_download.ipynb --stdout | grep -c "assert" | xargs -I {} test {} -ge 10 && echo "PASS: 10+ assertions found"</automated>
  </verify>
  <acceptance_criteria>
    - File notebooks/00_download.ipynb exists
    - Notebook contains at least 10 assert statements (one per dataset + summary)
    - Each of the 9 datasets has a dedicated download cell with URL, save path, and validation
    - La Poste table has its own download cell
    - Summary cell at the end prints file status table
    - RPPS download uses urlretrieve (not requests) due to file size
    - Each download is wrapped in try/except
    - Downloads are skippable if file already exists (re-runnable)
  </acceptance_criteria>
  <done>00_download.ipynb exists with 12+ cells covering all 9 datasets + La Poste + setup + summary, each with inline assertions</done>
</task>

<task type="auto">
  <name>Task 2: Run the download notebook and fix any broken URLs</name>
  <files>notebooks/00_download.ipynb</files>
  <read_first>
    - notebooks/00_download.ipynb
    - datasets-santescope.md
  </read_first>
  <action>
Execute the notebook cell by cell using `jupyter execute` or by running the equivalent Python.

For each dataset:
1. Run the download cell
2. If the URL is broken (404, redirect to HTML page, etc.), browse the dataset page URL from datasets-santescope.md to find the correct direct download link
3. Update the notebook cell with the working URL
4. Re-run and confirm the assertion passes

**Expected issues to handle:**
- data.gouv.fr pages often have multiple resources — pick the right one (CSV, not documentation PDF)
- INSEE pages may require navigating to find the direct download link
- Admin Express may come as a large ZIP — extract just the communes GeoJSON
- data.ameli.fr may use an API export endpoint rather than direct CSV download
- RPPS is ~761MB — this download will take several minutes, be patient

**After all downloads complete:**
- Run the summary cell
- Verify all 10 files exist in `notebooks/data/raw/`
- Print file sizes to confirm nothing is suspiciously small

**If a dataset is truly unavailable** (site down, format changed), document it in a markdown cell and create a placeholder with a TODO. Do not block the entire notebook.
  </action>
  <verify>
    <automated>ls -la notebooks/data/raw/ | wc -l | xargs -I {} test {} -ge 10 && echo "PASS: 10+ files in raw/"</automated>
  </verify>
  <acceptance_criteria>
    - At least 9 files exist in notebooks/data/raw/ (the 9 datasets)
    - La Poste correspondence file exists in notebooks/data/raw/
    - No file is 0 bytes: `find notebooks/data/raw/ -empty` returns nothing
    - RPPS file is >100MB: `ls -la notebooks/data/raw/rpps.csv` shows >100000000 bytes
    - All notebook assertions pass when run (no AssertionError in output)
  </acceptance_criteria>
  <done>All 9 datasets + La Poste table downloaded in notebooks/data/raw/, all assertions pass, summary cell shows all OK</done>
</task>

</tasks>

<verification>
- `ls notebooks/data/raw/` shows 10+ files (9 datasets + La Poste table)
- No file is 0 bytes
- `jupyter nbconvert --execute notebooks/00_download.ipynb` completes without error
- RPPS file is >100MB
- La Poste file has >30K rows
</verification>

<success_criteria>
All 9 source datasets and the La Poste correspondence table are downloaded into notebooks/data/raw/ with integrity validated by inline assertions. The download notebook is re-runnable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md`
</output>
