---
phase: 04-integration-deploy
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - santescope/README.md
  - santescope/public/pitch/index.html
autonomous: false
requirements:
  - DEP-01
  - DEP-03
must_haves:
  truths:
    - "Code is on a public GitHub repository"
    - "App is deployed on Vercel at a public URL"
    - "README describes the project, stack, data sources, and includes deployed URL"
    - "Pitch slides are accessible at /pitch/ on the deployed URL"
    - "5 demo communes work on the deployed URL"
  artifacts:
    - path: "santescope/README.md"
      provides: "Project documentation for hackathon jury"
      contains: "SanteScope"
    - path: "santescope/public/pitch/index.html"
      provides: "Reveal.js pitch slides"
      contains: "reveal.js"
  key_links:
    - from: "GitHub repository"
      to: "Vercel deployment"
      via: "git-based deploy"
      pattern: "vercel.app"
    - from: "santescope/public/pitch/index.html"
      to: "https://santescope.vercel.app/pitch/"
      via: "static file serving"
      pattern: "reveal"
user_setup:
  - service: vercel
    why: "Hosting the deployed app"
    env_vars: []
    dashboard_config:
      - task: "Import GitHub repo as new project"
        location: "vercel.com -> New Project -> Import Git Repository"
      - task: "Set Root Directory to 'santescope'"
        location: "Project settings during import"
  - service: github
    why: "Code hosting (DEP-03)"
    env_vars: []
    dashboard_config:
      - task: "Verify gh CLI is authenticated"
        location: "Terminal: gh auth status"
---

<objective>
Create GitHub repo, deploy to Vercel, write README and pitch slides for hackathon submission.

Purpose: DEP-01 (Vercel deploy), DEP-03 (GitHub repo), and D-07/D-08/D-09 (submission dossier). The app is already functional after Plan 01 fixes.
Output: Public GitHub repo, live Vercel URL, README, Reveal.js pitch slides at /pitch/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integration-deploy/04-CONTEXT.md
@.planning/phases/04-integration-deploy/04-RESEARCH.md
@.planning/phases/04-integration-deploy/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHub repo + Vercel deploy + README</name>
  <files>
    santescope/README.md
  </files>
  <read_first>
    santescope/README.md,
    santescope/package.json,
    .planning/PROJECT.md,
    .planning/phases/04-integration-deploy/04-RESEARCH.md
  </read_first>
  <action>
**1. Write `santescope/README.md`** (per D-09) with this structure:

```markdown
# SanteScope

Jumeau numerique territorial pour la sante des communes francaises.
SanteScope identifie les fragilites sanitaires d'une commune et propose des pistes d'action inspirees de communes similaires qui ont ameliore leur situation.

## Demo

**URL** : https://santescope.vercel.app
**Pitch** : https://santescope.vercel.app/pitch/

## Fonctionnalites

- Recherche parmi 34 969 communes (autocomplete < 150ms)
- Diagnostic sante : score de vulnerabilite 0-10, detail des 4 composantes
- Projection "domino" : impact des departs a la retraite des medecins
- Jumelles territoriales : communes similaires ayant ameliore leur situation
- Comparaison libre entre 2 communes
- Export PDF du diagnostic

## Stack technique

Next.js 16 . TypeScript . Tailwind CSS . shadcn/ui . html2canvas

## Donnees ouvertes

| Source | Producteur | Utilisation |
|--------|-----------|-------------|
| APL | DREES | Accessibilite aux soins |
| RPPS | Sante.fr | Effectifs medicaux par commune |
| FiLoSoFi 2018 | INSEE | Taux de pauvrete |
| RP 2020 | INSEE | Structure d'age (75+) |
| SAE Urgences | DREES | Temps d'acces aux urgences |
| FINESS | Sante.gouv | Etablissements de sante |
| Pathologies | Ameli | Prevalence departementale |
| DREES Demographie | DREES | Age des medecins par dept |
| La Poste | La Poste | Code postal -> code commune |

## Methodologie

Le score de vulnerabilite combine 4 indicateurs normalises (min-max) :
1. APL inverse (accessibilite aux soins)
2. Taux de pauvrete
3. Part des 75+ vivant seuls
4. Temps d'acces aux urgences

Les jumelles sont identifiees par KNN sur 5 features normalisees avec priorite regionale.
Voir `notebooks/` pour le pipeline complet.

## Lancement local

git clone https://github.com/{user}/santescope.git
cd santescope
npm install
npm run dev
# Ouvrir http://localhost:3000
```

Replace `{user}` with the actual GitHub username from `gh api user -q .login`.

**2. Commit all changes** (including the 35K JSON files from plan 01 gitignore change):
- `git add santescope/public/data/` — this adds ~155MB of JSON files
- `git add santescope/README.md`
- `git add santescope/scripts/smoke-test.js`
- `git add -A` for any remaining changes
- Commit with message: `feat(04): prepare for deploy — README, data files, smoke test`

**3. Create GitHub repo** (per DEP-03):
- `gh auth status` — verify authenticated
- `gh repo create santescope --public --source=. --remote=origin --push`
- If repo already exists, just `git remote add origin` + `git push -u origin master`
- Note: the push will take 3-5 minutes for 155MB. Use `--verbose` if available.

**4. Verify push:**
- `gh repo view --web` or `gh repo view` to confirm repo exists
  </action>
  <verify>
    <automated>gh repo view 2>&1 | head -5 && echo "---" && test -f /Users/zakariyahamdouchi/Dev/Hackathon/santescope/README.md && grep "SanteScope" /Users/zakariyahamdouchi/Dev/Hackathon/santescope/README.md && echo "README OK"</automated>
  </verify>
  <acceptance_criteria>
    - santescope/README.md contains "SanteScope"
    - santescope/README.md contains "vercel.app"
    - santescope/README.md contains "DREES" and "RPPS" and "INSEE"
    - santescope/README.md contains "npm run dev"
    - `gh repo view` shows the repo exists on GitHub
    - `git log --oneline -1` shows a commit with data files
  </acceptance_criteria>
  <done>Code is on GitHub (DEP-03). README documents the project with stack, data sources, and instructions.</done>
</task>

<task type="auto">
  <name>Task 2: Reveal.js pitch slides</name>
  <files>
    santescope/public/pitch/index.html
  </files>
  <read_first>
    .planning/PROJECT.md,
    .planning/phases/04-integration-deploy/04-CONTEXT.md,
    .planning/phases/04-integration-deploy/04-RESEARCH.md
  </read_first>
  <action>
**Create `santescope/public/pitch/index.html`** (per D-08) — Standalone Reveal.js presentation accessible at /pitch/. Use CDN (no npm install).

Structure (8-10 slides):

1. **Titre** — "SanteScope" + tagline "Le jumeau numerique de la sante communale" + logo concept (CSS only)
2. **Probleme** — Les deserts medicaux s'aggravent. 6M de Francais sans medecin traitant. Les elus locaux manquent de donnees actionnables.
3. **Solution** — Diagnostic automatique + jumelles territoriales. "Montrer le probleme ET ce qu'une commune similaire a fait pour le resoudre."
4. **Demo screenshot** — Placeholder for screenshot or embed of the app URL. Include link to live demo.
5. **Donnees** — 9 sources open data croisees, 34 969 communes couvertes, score de vulnerabilite 0-10.
6. **Methodologie** — Score 4 composantes, KNN jumelles, projection domino retraites medecins.
7. **Impact** — Un elu peut : identifier ses fragilites, decouvrir des solutions testees ailleurs, exporter un rapport PDF pour son conseil municipal.
8. **Stack** — Next.js 16, TypeScript, Tailwind, donnees statiques (pas de backend), deploye sur Vercel.
9. **Equipe** — Placeholder for team info.
10. **Merci + liens** — URL de la demo, GitHub, contact.

Styling:
- Use Reveal.js 5.2.1 from JSDelivr CDN (confirmed stable)
- White theme with custom CSS overrides: primary color #0F766E (teal, matches app), headings in Inter font (Google Fonts CDN)
- Clean, minimal slides — no animations per D-01
- Responsive (Reveal.js handles this natively)

```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/reveal.js@5.2.1/dist/reveal.css">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/reveal.js@5.2.1/dist/theme/white.css">
<script src="https://cdn.jsdelivr.net/npm/reveal.js@5.2.1/dist/reveal.esm.js" type="module"></script>
```

Commit and push: `git add santescope/public/pitch/ && git commit -m "feat(04): add Reveal.js pitch slides" && git push`
  </action>
  <verify>
    <automated>test -f /Users/zakariyahamdouchi/Dev/Hackathon/santescope/public/pitch/index.html && grep "reveal" /Users/zakariyahamdouchi/Dev/Hackathon/santescope/public/pitch/index.html && grep "SanteScope" /Users/zakariyahamdouchi/Dev/Hackathon/santescope/public/pitch/index.html && echo "PITCH OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `santescope/public/pitch/index.html` exists
    - Contains `reveal.js@5.2.1` CDN link
    - Contains `SanteScope` in title/first slide
    - Contains at least 8 `<section>` elements (slides)
    - Contains `0F766E` (brand teal color)
    - File is committed and pushed to GitHub
  </acceptance_criteria>
  <done>Pitch slides accessible at /pitch/ route. Reveal.js presentation with 8-10 slides covering problem, solution, data, methodology, and impact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deploy to Vercel + verify live site</name>
  <files>none — human action in Vercel dashboard</files>
  <action>
Human deploys the app via Vercel dashboard (cannot be automated — requires Vercel account authentication and UI interaction for first-time project setup). Claude runs the smoke test against the production URL after deployment.
  </action>
  <what-built>
    GitHub repo with full code + 35K JSON data files, README, and pitch slides. Ready for Vercel import.
  </what-built>
  <how-to-verify>
    1. Go to https://vercel.com/new
    2. Import the GitHub repository (santescope)
    3. CRITICAL: Set "Root Directory" to `santescope` (not repo root — this is a monorepo)
    4. Framework: Next.js (auto-detected)
    5. Click Deploy — wait for build (may take 2-5 min with 35K static files)
    6. Once deployed, verify:
       - Visit the Vercel URL (e.g., https://santescope.vercel.app)
       - Search for "Paris" -> click -> page loads without crash, shows "Non disponible" for APL
       - Search for "Saint-Quentin" -> click -> full diagnostic with score
       - Visit /pitch/ -> slides render with Reveal.js navigation
       - Test PDF download on any commune
       - Test comparison mode: navigate to /comparer/02691/59392
    7. Run smoke test against prod: `BASE_URL=https://santescope.vercel.app node santescope/scripts/smoke-test.js`
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: requires human Vercel deploy. After deploy, run: BASE_URL=https://santescope.vercel.app node santescope/scripts/smoke-test.js"</automated>
  </verify>
  <done>App is live at a public Vercel URL. All 5 demo communes load. Pitch slides render. DEP-01 satisfied.</done>
  <resume-signal>Type "deployed" with the Vercel URL, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. `gh repo view` — repo exists on GitHub with all code
2. Vercel URL returns 200 with SanteScope content
3. 5 demo communes load without crash on production URL
4. /pitch/ route shows Reveal.js slides
5. `BASE_URL=https://... node santescope/scripts/smoke-test.js` — all pass
</verification>

<success_criteria>
- DEP-01: App deployed on Vercel with public URL
- DEP-03: Code on GitHub repository
- D-07/D-08: README + pitch slides present
- D-09: README includes description, stack, launch instructions, data sources
- D-10: 5 demo communes verified on production
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-deploy/04-02-SUMMARY.md`
</output>
