---
phase: 04-integration-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - santescope/src/lib/types.ts
  - santescope/src/components/commune/ScoreDetail.tsx
  - santescope/src/components/commune/TwinPanel.tsx
  - santescope/src/components/commune/CommuneEquipments.tsx
  - santescope/src/components/commune/ActionPathways.tsx
  - santescope/scripts/smoke-test.js
  - santescope/.gitignore
autonomous: true
requirements:
  - DEP-02
must_haves:
  truths:
    - "Paris (75056) page loads without crash"
    - "All 5 demo communes load without crash"
    - "Null score_detail fields display 'N/D' or 'Non disponible' instead of crashing"
    - "35K JSON files are tracked by git (not gitignored)"
    - "Smoke test script validates 5 communes automatically"
  artifacts:
    - path: "santescope/src/lib/types.ts"
      provides: "Nullable types for apl, temps_urgences_min, has_hopital, has_ehpad, nb_etablissements, apl_evolution"
      contains: "apl: number | null"
    - path: "santescope/scripts/smoke-test.js"
      provides: "Automated smoke test for 5 communes"
      contains: "75056"
  key_links:
    - from: "santescope/src/lib/types.ts"
      to: "ScoreDetail.tsx, TwinPanel.tsx, CommuneEquipments.tsx"
      via: "CommuneData import"
      pattern: "number \\| null"
    - from: "santescope/scripts/smoke-test.js"
      to: "public/data/communes/*.json"
      via: "fetch validation"
      pattern: "fetch.*communes"
---

<objective>
Fix null-safety bugs that crash the app on Paris and edge-case communes, create automated smoke test, and prepare git for data commit.

Purpose: The app currently crashes on /commune/75056 (Paris) because score_detail.apl and temps_urgences_min are null. This must be fixed before any deploy or demo. The gitignore exclusion for public/data/ must be removed so Vercel can serve the JSONs (per D-06).
Output: Crash-free app for all 5 demo communes, smoke test script, gitignore updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integration-deploy/04-CONTEXT.md
@.planning/phases/04-integration-deploy/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From santescope/src/lib/types.ts (CURRENT — has bugs):
```typescript
export interface CommuneData {
  score_detail: {
    apl: number;                    // BUG: should be number | null
    apl_national: number;
    pauvrete: number | null;        // already nullable (correct)
    pauvrete_national: number;
    pct_75_seuls: number;
    pct_75_seuls_national: number;
    temps_urgences_min: number;     // BUG: should be number | null
    temps_urgences_national: number;
  };
  has_hopital: boolean;             // BUG: should be boolean | null
  has_ehpad: boolean;               // BUG: should be boolean | null
  nb_etablissements: number;        // BUG: should be number | null
  apl_evolution: Record<string, number>;  // BUG: should be Record<string, number> | null
}
```

Paris (75056) real JSON null fields:
- score: null, apl: null, temps_urgences_min: null
- has_hopital: null, has_ehpad: null, nb_etablissements: null, apl_evolution: null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix null-safety types and component guards</name>
  <files>
    santescope/src/lib/types.ts,
    santescope/src/components/commune/ScoreDetail.tsx,
    santescope/src/components/commune/TwinPanel.tsx,
    santescope/src/components/commune/CommuneEquipments.tsx,
    santescope/src/components/commune/ActionPathways.tsx,
    santescope/src/components/commune/CommuneView.tsx
  </files>
  <read_first>
    santescope/src/lib/types.ts,
    santescope/src/components/commune/ScoreDetail.tsx,
    santescope/src/components/commune/TwinPanel.tsx,
    santescope/src/components/commune/CommuneEquipments.tsx,
    santescope/src/components/commune/ActionPathways.tsx,
    santescope/src/components/commune/CommuneView.tsx,
    santescope/public/data/communes/75056.json
  </read_first>
  <action>
**1. Update types in `santescope/src/lib/types.ts`** — change these fields in CommuneData:
- `score_detail.apl: number` -> `apl: number | null`
- `score_detail.temps_urgences_min: number` -> `temps_urgences_min: number | null`
- `has_hopital: boolean` -> `has_hopital: boolean | null`
- `has_ehpad: boolean` -> `has_ehpad: boolean | null`
- `nb_etablissements: number` -> `nb_etablissements: number | null`
- `apl_evolution: Record<string, number>` -> `apl_evolution: Record<string, number> | null`

**2. Fix ScoreDetail.tsx** — Lines 123-124 and 145-146:
- Line 124: change `scoreDetail.apl.toFixed(1)` to `scoreDetail.apl != null ? scoreDetail.apl.toFixed(1) : "Non disponible"` and add `unavailable: scoreDetail.apl == null`
- Line 146: change `scoreDetail.temps_urgences_min.toFixed(0) + " min"` to `scoreDetail.temps_urgences_min != null ? scoreDetail.temps_urgences_min.toFixed(0) + " min" : "Non disponible"` and add `unavailable: scoreDetail.temps_urgences_min == null`
- Follow the exact same pattern already used for `pauvrete` on lines 130-136

**3. Fix TwinPanel.tsx** — Line 241, 243, 273:
- Line 241: change `commune.score_detail.apl.toFixed(1)` to `commune.score_detail.apl != null ? commune.score_detail.apl.toFixed(1) : "N/D"`
- Line 243 CompareValue: guard `myVal` and `twinVal` — if either is null, render "N/D" instead of calling `.toFixed()`
- Line 273: change `commune.score_detail.temps_urgences_min.toFixed(0) + " min"` to `commune.score_detail.temps_urgences_min != null ? commune.score_detail.temps_urgences_min.toFixed(0) + " min" : "N/D"`. Same for twinData on that line.
- AplSparkline (line 250-251): Guard against null apl_evolution — if `commune.apl_evolution` is null, pass empty object `{}` instead. Same for `twinData.apl_evolution`.

**4. Fix CommuneEquipments.tsx** — Lines 10-11:
- When `data.has_hopital` is null, treat as unknown (not present, not absent). Filter out null entries from equipments display or add a 3rd "Donnees indisponibles" category.
- Simplest approach: filter out null entries: `{ label: "Hopital", present: data.has_hopital }` and only include in `has`/`missing` if value is not null. Add a `const unknown = equipments.filter(e => e.present === null)` section with gray "Donnees non disponibles" styling.

**5. Fix ActionPathways.tsx** — Line 82:
- Change `!d.has_ehpad && d.score_detail.pct_75_seuls > 0.10` to `d.has_ehpad === false && d.score_detail.pct_75_seuls > 0.10` (explicit false check, not truthy check on null)
- Review all other `trigger` functions in the file for similar null-unsafe patterns (e.g., `!d.msp_presente` should be `d.msp_presente === false`)

**6. Fix CommuneView.tsx** — Line 197:
- Guard `data.apl_evolution` before passing to ScoreDetail: `aplEvolution={data.apl_evolution ?? {}}`.
  </action>
  <verify>
    <automated>cd /Users/zakariyahamdouchi/Dev/Hackathon/santescope && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - types.ts contains `apl: number | null`
    - types.ts contains `temps_urgences_min: number | null`
    - types.ts contains `has_hopital: boolean | null`
    - types.ts contains `has_ehpad: boolean | null`
    - types.ts contains `nb_etablissements: number | null`
    - types.ts contains `apl_evolution: Record<string, number> | null`
    - ScoreDetail.tsx contains `scoreDetail.apl != null`
    - ScoreDetail.tsx contains `scoreDetail.temps_urgences_min != null`
    - TwinPanel.tsx line with `.apl.toFixed` replaced with null guard
    - CommuneEquipments.tsx handles null present values
    - ActionPathways.tsx uses `=== false` not `!d.has_ehpad`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All null-safety bugs fixed. Paris (75056) page renders without crash. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke test script + gitignore surgery</name>
  <files>
    santescope/scripts/smoke-test.js,
    santescope/.gitignore
  </files>
  <read_first>
    santescope/.gitignore,
    santescope/public/data/communes/75056.json,
    santescope/public/data/communes/02691.json
  </read_first>
  <action>
**1. Create `santescope/scripts/smoke-test.js`** (per D-11) — Node.js script that:
```javascript
const BASE = process.env.BASE_URL || 'http://localhost:3000';
const COMMUNES = [
  { code: '75056', name: 'Paris', expectNullScore: true },
  { code: '02691', name: 'Saint-Quentin', expectNullScore: false },
  { code: '59392', name: 'Maubeuge', expectNullScore: false },
  { code: '08409', name: 'Sedan', expectNullScore: false },
  { code: '52123', name: 'Chevillon', expectNullScore: false },
];
```
For each commune:
- `fetch(BASE + '/data/communes/' + code + '.json')` — assert 200
- Parse JSON, assert fields: `code`, `nom`, `dept`, `data_quality`, `score_detail`, `coords`, `jumelles` (array)
- For non-null-score communes: assert `score` is a number, `classe` is A-E
- For Paris: assert `score === null`, `score_detail.apl === null`, `score_detail.temps_urgences_min === null`
- Also fetch `BASE + '/data/index.json'` — assert 200, assert array length > 30000
- Print pass/fail per commune + summary. Exit code 1 if any failure.
- Use native `fetch` (Node 18+). No dependencies.

**2. Edit `santescope/.gitignore`** (per D-06) — Remove these 2 lines:
```
/public/data/communes/
/public/data/index.json
```
Keep the comment line `# Data files (large, generated by notebooks — not versioned)` but change it to `# Data files (generated by notebooks)` since they ARE now versioned.
  </action>
  <verify>
    <automated>cd /Users/zakariyahamdouchi/Dev/Hackathon/santescope && node scripts/smoke-test.js 2>&1 || echo "NOTE: smoke test against localhost requires dev server running — verify script file exists and has correct structure"</automated>
  </verify>
  <acceptance_criteria>
    - File `santescope/scripts/smoke-test.js` exists
    - smoke-test.js contains `75056` and `02691` and `59392` and `08409` and `52123`
    - smoke-test.js contains `fetch` and `process.env.BASE_URL`
    - smoke-test.js checks `index.json` with length > 30000
    - .gitignore does NOT contain `/public/data/communes/`
    - .gitignore does NOT contain `/public/data/index.json`
    - `git status santescope/public/data/` shows untracked files (data now visible to git)
  </acceptance_criteria>
  <done>Smoke test script created and validates 5 communes. Gitignore updated so public/data/ is tracked by git.</done>
</task>

</tasks>

<verification>
1. `cd santescope && npx tsc --noEmit` — zero errors
2. `cd santescope && npm run build` — build succeeds
3. Start dev server, visit http://localhost:3000/commune/75056 — no crash, shows "Non disponible" for APL and urgences
4. `node santescope/scripts/smoke-test.js` — all 5 communes pass (with dev server running)
5. `git status santescope/public/data/ | head -5` — shows untracked files
</verification>

<success_criteria>
- Paris page loads without crash
- All 5 demo communes render correctly with null fields showing fallback text
- Smoke test script exists and validates JSON structure
- public/data/ is no longer gitignored
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-deploy/04-01-SUMMARY.md`
</output>
