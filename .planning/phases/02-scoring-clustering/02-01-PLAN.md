---
phase: 02-scoring-clustering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - notebooks/02_score.ipynb
  - notebooks/03_domino.ipynb
  - notebooks/data/processed/communes_master.parquet
autonomous: true
requirements: [SCORE-01, SCORE-02, SCORE-03, SCORE-04, DOC-01, DOC-02, DOC-03, DOM-01, DOM-02, DOM-03]

must_haves:
  truths:
    - "Vulnerability score 0-10 computed for >99% of communes (34815+)"
    - "Communes with <3 of 4 components get score = null (~154 communes)"
    - "A-E classification assigned to every scored commune"
    - "Each commune exposes 4 component values + national medians"
    - "Missing specialties identified for communes with RPPS data"
    - "Domino alert computed for communes with >3 doctors"
    - "Region and density columns added for twin matching"
  artifacts:
    - path: "notebooks/02_score.ipynb"
      provides: "Vulnerability score computation"
      contains: "score_final"
    - path: "notebooks/03_domino.ipynb"
      provides: "Domino projection + missing doctors + GeoJSON enrichment"
      contains: "domino_alert"
    - path: "notebooks/data/processed/communes_master.parquet"
      provides: "Enriched master with score, classe, domino, manques, region, density columns"
  key_links:
    - from: "notebooks/02_score.ipynb"
      to: "notebooks/data/processed/communes_master.parquet"
      via: "read parquet -> add score columns -> save back"
      pattern: "to_parquet.*communes_master"
    - from: "notebooks/03_domino.ipynb"
      to: "notebooks/data/processed/communes_master.parquet"
      via: "read parquet -> add domino+geo columns -> save back"
      pattern: "to_parquet.*communes_master"
---

<objective>
Compute the vulnerability score (4 components, min-max normalized, 0-10, A-E classification) and the domino projection (retirement risk, missing doctors) for all communes. Also enrich the parquet with GeoJSON-derived region and density columns needed by the twin matching notebook.

Purpose: These derived features are the analytical core of SanteScope — scoring quantifies health vulnerability, domino forecasts doctor shortages, missing specialties pinpoint care gaps.

Output: `02_score.ipynb`, `03_domino.ipynb`, enriched `communes_master.parquet` with ~15 new columns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scoring-clustering/02-CONTEXT.md
@.planning/phases/02-scoring-clustering/02-RESEARCH.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Parquet columns available from Phase 1 (29 columns) -->
From notebooks/data/processed/communes_master.parquet:
- code_commune (str): INSEE COG 5-digit, e.g. "02691"
- nom_commune (str): commune name
- population (int): total population
- code_departement (str): "02", "75", "2A", etc.
- nb_generalistes (int): GP count from RPPS
- nb_medecins_total (int): total doctor count from RPPS
- nb_specialistes (int): specialist count from RPPS
- specialistes_detail (str, JSON dict): e.g. '{"cardiologue": 2, "psychiatre": 1}', null for 88.4%
- apl_2022 (float): APL value 2022, null 0.4%
- apl_2023 (float): APL value 2023, null 0.4%
- apl (float): APL latest (=apl_2023), null 0.4%
- apl_evolution (float): apl_2023 - apl_2022, null 0.4%
- taux_pauvrete (float): poverty rate, null 87.8% (FiLoSoFi secrecy)
- revenu_median (float): median income, null 10.8%
- pct_75_plus (float): % population 75+, null 0.4%
- temps_urgences_min (float): emergency access time in minutes, null 0.4%
- temps_urgences_mcs_min (float): MCS emergency time, null 0.3%
- nb_etablissements (int): health establishment count
- has_hopital (bool): hospital present
- has_ehpad (bool): EHPAD present
- nb_msp (int): MSP count
- has_msp (bool): MSP present
- latitude (float): centroid lat, null 0.3%
- longitude (float): centroid lon, null 0.3%
- prev_diabete..prev_respiratoire (float): dept-level pathology prevalence rates, null 1.3%

From notebooks/data/raw/communes_geo.geojson:
- properties.codgeo (str): commune code for join
- properties.reg (str): region code, e.g. "84"
- properties.superf (int): surface in hectares
- 34,879 features (vs 34,969 in parquet)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 02_score.ipynb — vulnerability score computation</name>
  <files>notebooks/02_score.ipynb</files>

  <read_first>
    - notebooks/data/processed/communes_master.parquet (current columns and data)
    - .planning/phases/02-scoring-clustering/02-RESEARCH.md (score patterns and anti-patterns)
    - .planning/phases/02-scoring-clustering/02-CONTEXT.md (decisions D-01 through D-06)
    - .planning/phases/01-data-foundation/01-CONTEXT.md (frozen JSON schema for score_detail format)
    - notebooks/01_merge.ipynb (established notebook patterns: path detection, assertion style)
  </read_first>

  <action>
Create `notebooks/02_score.ipynb` using nbformat (same pattern as 01_merge.ipynb). The notebook reads `communes_master.parquet`, computes the vulnerability score, and saves back.

**Notebook structure (markdown + code cells):**

**Section 1: Setup + Load**
- Path-agnostic detection (same pattern as 01_merge): detect if cwd is `notebooks/` or project root, set PROCESSED path accordingly
- `import pandas as pd, numpy as np`
- `df = pd.read_parquet(PROCESSED / 'communes_master.parquet')`
- Print shape and null rates for the 4 score components

**Section 2: APL Inversion (per D-01, SCORE-01)**
- Markdown cell documenting the method: "Inversion par (max - apl): evite la division par zero pour les 517 communes avec APL=0"
- `apl_inv = df['apl'].max() - df['apl']` (NOT 1/apl — 517 communes have APL=0)
- For communes with apl=NaN: leave as NaN (they will be imputed in normalization step)

**Section 3: Min-Max Normalization (per D-02, SCORE-02)**
- Define `minmax_normalize(series)` function:
  ```python
  def minmax_normalize(series):
      mn, mx = series.min(), series.max()
      if mx == mn:
          return pd.Series(0.5, index=series.index)
      return (series - mn) / (mx - mn)
  ```
- Impute missing values with component median BEFORE normalizing (per research: so they land at ~0.5 = neutral)
- Compute 4 normalized components:
  - `score_apl_norm = minmax_normalize(apl_inv.fillna(apl_inv.median()))`
  - `score_pauvrete_norm = minmax_normalize(df['taux_pauvrete'].fillna(df['taux_pauvrete'].median()))`
  - `score_pct75_norm = minmax_normalize(df['pct_75_plus'].fillna(df['pct_75_plus'].median()))`
  - `score_urgences_norm = minmax_normalize(df['temps_urgences_min'].fillna(df['temps_urgences_min'].median()))`

**Section 4: Score Computation (per D-01, D-03, SCORE-01, SCORE-03)**
- Count available components per commune:
  ```python
  n_components = (df[['apl','taux_pauvrete','pct_75_plus','temps_urgences_min']].notna()).sum(axis=1)
  ```
- Compute raw score: `score_raw = (score_apl_norm + score_pauvrete_norm + score_pct75_norm + score_urgences_norm) / 4` (ALWAYS divide by 4, per D-01 equal 25% weights)
- Scale to 0-10: `score_final = (score_raw * 10).round(1)`
- Null out communes with <3 components: `score_final[n_components < 3] = np.nan` (per D-03)
- Add column `df['score'] = score_final`
- Add column `df['n_score_components'] = n_components`
- Store normalized components: `df['score_apl_norm'] = score_apl_norm`, same for the other 3

**Section 5: A-E Classification (per D-05)**
- Markdown cell: "Classification A-E inspiree du DPE. Seuils quintiles: chaque classe regroupe ~20% des communes scorees."
- Compute quintile thresholds: `thresholds = np.nanpercentile(df['score'].dropna(), [20, 40, 60, 80])`
- Print thresholds and document in markdown
- Define classify function:
  ```python
  def classify(score, t):
      if pd.isna(score): return None
      if score <= t[0]: return 'A'
      if score <= t[1]: return 'B'
      if score <= t[2]: return 'C'
      if score <= t[3]: return 'D'
      return 'E'
  ```
- `df['classe'] = df['score'].apply(lambda s: classify(s, thresholds))`

**Section 6: National Medians (per D-04, D-06, SCORE-04)**
- Compute medians from raw (non-imputed) values:
  - `median_apl = df['apl'].median()`
  - `median_pauvrete = df['taux_pauvrete'].median()`
  - `median_pct75 = df['pct_75_plus'].median()`
  - `median_urgences = df['temps_urgences_min'].median()`
- Store as metadata (these will be embedded in each commune JSON during export):
  - `NATIONAL_MEDIANS = {'apl': median_apl, 'pauvrete': median_pauvrete, 'pct_75_seuls': median_pct75, 'temps_urgences_min': median_urgences}`
  - Save to `notebooks/data/processed/national_medians.json`

**Section 7: Validation Assertions**
- `assert df['score'].notna().sum() > 34000, f"Score computed for {df['score'].notna().sum()} communes (expected >34000)"`
- `assert df['score'].dropna().between(0, 10).all(), "Score out of [0, 10] range"`
- `assert (df['score'][n_components < 3].isna()).all(), "Communes with <3 components should have null score"`
- `assert df['classe'].dropna().isin(['A','B','C','D','E']).all(), "Invalid classification"`
- `assert set(df['classe'].dropna().unique()) == {'A','B','C','D','E'}, "Not all classes represented"`
- Print distribution: `df['classe'].value_counts().sort_index()`
- Print score describe: `df['score'].describe()`

**Section 8: Save**
- `df.to_parquet(PROCESSED / 'communes_master.parquet', index=False)`
- Print new column list and shape

Execute the notebook with `jupyter nbconvert --to notebook --execute`.
  </action>

  <verify>
    <automated>cd /Users/zakariyahamdouchi/Dev/Hackathon && python3 -c "
import pandas as pd, json
df = pd.read_parquet('notebooks/data/processed/communes_master.parquet')
assert 'score' in df.columns, 'score column missing'
assert 'classe' in df.columns, 'classe column missing'
assert df['score'].notna().sum() > 34000, f'Only {df[\"score\"].notna().sum()} scored'
assert df['score'].dropna().between(0, 10).all(), 'Score out of range'
assert set(df['classe'].dropna().unique()) == {'A','B','C','D','E'}, 'Missing classes'
import pathlib
assert pathlib.Path('notebooks/data/processed/national_medians.json').exists(), 'national_medians.json missing'
medians = json.load(open('notebooks/data/processed/national_medians.json'))
assert all(k in medians for k in ['apl','pauvrete','pct_75_seuls','temps_urgences_min']), 'Missing median keys'
print('ALL SCORE CHECKS PASSED')
"</automated>
  </verify>

  <acceptance_criteria>
    - notebooks/02_score.ipynb exists with executed output cells
    - communes_master.parquet contains columns: score, classe, n_score_components, score_apl_norm, score_pauvrete_norm, score_pct75_norm, score_urgences_norm
    - df['score'].notna().sum() > 34000
    - df['score'].dropna().between(0, 10).all() is True
    - df['classe'].dropna().unique() contains exactly {'A','B','C','D','E'}
    - notebooks/data/processed/national_medians.json exists with keys apl, pauvrete, pct_75_seuls, temps_urgences_min
    - Score = NaN for communes with n_score_components < 3
  </acceptance_criteria>

  <done>Vulnerability score 0-10 computed for >34000 communes, A-E classification assigned, national medians exported, parquet updated</done>
</task>

<task type="auto">
  <name>Task 2: Create 03_domino.ipynb — domino projection, missing doctors, GeoJSON enrichment</name>
  <files>notebooks/03_domino.ipynb, notebooks/data/processed/communes_master.parquet</files>

  <read_first>
    - notebooks/data/processed/communes_master.parquet (after Task 1 — has score columns)
    - notebooks/data/raw/communes_geo.geojson (region and surface data)
    - .planning/phases/02-scoring-clustering/02-RESEARCH.md (DREES download pattern, dept code format, pitfalls 3+4)
    - .planning/phases/02-scoring-clustering/02-CONTEXT.md (decisions D-07 through D-13)
    - .planning/phases/01-data-foundation/01-CONTEXT.md (frozen JSON schema for domino and manques format)
    - notebooks/01_merge.ipynb (established patterns)
  </read_first>

  <action>
Create `notebooks/03_domino.ipynb` using nbformat. This notebook handles 3 things: (A) GeoJSON enrichment (region, surface, density), (B) missing doctor detection, (C) domino projection.

**Section 1: Setup + Load**
- Path-agnostic detection (same pattern as 02_score)
- `import pandas as pd, numpy as np, json, io, urllib.request, openpyxl`
- `df = pd.read_parquet(PROCESSED / 'communes_master.parquet')`
- Print shape, confirm score columns exist from Task 1

**Section 2: GeoJSON Enrichment (needed for TWIN-01 density feature)**
- Markdown cell: "Enrichissement GeoJSON: region et superficie pour le calcul de densite (prerequis jumelles)"
- Load GeoJSON: `geo = json.load(open(RAW / 'communes_geo.geojson'))`
- Extract DataFrame: `geo_df = pd.DataFrame([f['properties'] for f in geo['features']])[['codgeo','reg','superf']]`
- Rename: `geo_df.rename(columns={'codgeo': 'code_commune', 'reg': 'region', 'superf': 'surface_ha'}, inplace=True)`
- Left-join: `df = df.merge(geo_df, on='code_commune', how='left')`
- Compute density: `df['densite_hab_km2'] = df['population'] / (df['surface_ha'] / 100)`
- Handle division by zero: `df.loc[df['surface_ha'] == 0, 'densite_hab_km2'] = np.nan`
- Assert: `df['region'].notna().sum() > 34000`
- Assert: `df['densite_hab_km2'].notna().sum() > 34000`

**Section 3: Missing Doctors Detection (per D-07, D-08, D-09, DOC-01..03)**
- Markdown cell: "Detection des specialites manquantes: croisement RPPS x pathologies departementales"
- Define pathology-to-specialty mapping (fixed table):
  ```python
  PATHO_SPECIALTY_MAP = {
      'prev_diabete': 'endocrinologue',
      'prev_cardio': 'cardiologue',
      'prev_psy': 'psychiatre',
      'prev_cancers': 'oncologue',
      'prev_respiratoire': 'pneumologue',
  }
  ```
- For each commune with specialistes_detail (non-null, ~11.6% of communes):
  - Parse JSON: `spec = json.loads(row['specialistes_detail'])` if string, else `row['specialistes_detail']` if dict
  - For each pathology column, check if corresponding specialty count is 0 or absent AND dept pathology rate is above national median
  - Build `manques` list: specialties where (commune has 0 of that specialty) AND (dept has above-median prevalence for the corresponding pathology)
- For communes WITHOUT specialistes_detail: `manques = None` (cannot assess)
- Store as list column: `df['manques'] = manques_series`
- Assert: communes with specialistes_detail have a list (possibly empty) as manques

**Section 4: DREES Download + Dept-level Age Data (per D-10, DOM-01)**
- Markdown cell: "Telechargement DREES: effectifs medecins par tranche d'age et departement"
- Download DREES XLSX with try/except fallback:
  ```python
  DREES_URL = (
      "https://data.drees.solidarites-sante.gouv.fr/api/explore/v2.1/catalog/datasets/"
      "la-demographie-des-professionnels-de-sante-depuis-2012/attachments/"
      "medecins_rpps_2012_2025_xlsx"
  )
  try:
      req = urllib.request.Request(DREES_URL, headers={"User-Agent": "Mozilla/5.0"})
      data = urllib.request.urlopen(req, timeout=120).read()
      df_drees = pd.read_excel(io.BytesIO(data), sheet_name='Effectifs', header=0)
      # Fix header: first row may be actual column names
      # Inspect and handle per research pitfall 3
      drees_downloaded = True
  except Exception as e:
      print(f"DREES download failed: {e}. Using national avg fallback.")
      drees_downloaded = False
  ```
- If downloaded: parse dept-level % 55+ doctors (2025 data):
  - Filter: `sexe == '0-Ensemble'`, `specialites_agregees == '00-Ensemble'`, `exercice == '0-Ensemble'`, `departement != '000-Ensemble'`
  - Total by dept: filter `tranche_age == '00-Ensemble'`, get `effectif_2025`
  - 55+ by dept: filter `tranche_age` starting with `'07','08','09','10','11'` (55-59, 60-64, 65-69, 70-74, 75+), sum `effectif_2025`
  - `pct_55plus_by_dept = (aged_55plus / total_by_dept)`
  - Extract dept code from labels like `'075 -Paris'` -> `'75'`: use `str.extract(r'0*(\d+[AB]?)')` then strip leading zeros for mainland, keep '2A'/'2B' for Corse
- If download failed: `pct_55plus_by_dept = defaultdict(lambda: 0.43)` (national avg 43%)

**Section 5: Domino Projection (per D-11, D-12, D-13, DOM-02, DOM-03)**
- Map dept pct_55plus to each commune via code_departement join
- `df['pct_55plus_dept'] = df['code_departement'].map(pct_55plus_by_dept)`
- Estimate 55+ doctors per commune: `df['medecins_55plus_est'] = (df['nb_medecins_total'] * df['pct_55plus_dept']).round(0)`
- National dept average: `avg_dept_pct55 = pct_55plus_by_dept.mean()` (or median)
- Domino alert (per D-11 double threshold): `df['domino_alert'] = (df['pct_55plus_dept'] > avg_dept_pct55) | (df['pct_55plus_dept'] > 0.5)`
- Only for communes with >3 doctors (per D-13): `df.loc[df['nb_medecins_total'] <= 3, 'domino_alert'] = None`
- Also null out medecins_55plus_est for communes with <=3 doctors
- Projection text: `df['projection_2030'] = df['medecins_55plus_est'].apply(lambda n: f"-{int(n)} medecins (estimation)" if pd.notna(n) and n > 0 else None)`
- Null projection for communes with <=3 doctors

**Section 6: Validation Assertions**
- `assert df['region'].notna().sum() > 34000, "Region join failed"`
- `assert df['densite_hab_km2'].notna().sum() > 34000, "Density computation failed"`
- `assert df['pct_55plus_dept'].notna().sum() > 4000, "DREES dept join failed"`
- `assert (df[df['nb_medecins_total'] > 3]['domino_alert'].notna()).sum() > 4000, "Domino alert missing for eligible communes"`
- `assert df.loc[df['nb_medecins_total'] <= 3, 'domino_alert'].isna().all(), "Domino alert should be null for <=3 doctor communes"`
- Print domino stats: how many communes with alert, projection distribution

**Section 7: Save**
- `df.to_parquet(PROCESSED / 'communes_master.parquet', index=False)`
- Print new column list and shape

Execute the notebook with `jupyter nbconvert --to notebook --execute` (timeout 300s for DREES download).
  </action>

  <verify>
    <automated>cd /Users/zakariyahamdouchi/Dev/Hackathon && python3 -c "
import pandas as pd
df = pd.read_parquet('notebooks/data/processed/communes_master.parquet')
assert 'region' in df.columns, 'region column missing'
assert 'densite_hab_km2' in df.columns, 'density column missing'
assert 'pct_55plus_dept' in df.columns, 'pct_55plus_dept missing'
assert 'domino_alert' in df.columns, 'domino_alert missing'
assert 'projection_2030' in df.columns, 'projection_2030 missing'
assert 'manques' in df.columns, 'manques missing'
assert 'medecins_55plus_est' in df.columns, 'medecins_55plus_est missing'
assert df['region'].notna().sum() > 34000, f'Region only {df[\"region\"].notna().sum()}'
assert df['densite_hab_km2'].notna().sum() > 34000, 'Density low'
assert df['pct_55plus_dept'].notna().sum() > 4000, 'DREES join low'
assert df.loc[df['nb_medecins_total'] <= 3, 'domino_alert'].isna().all(), 'Domino leak for <=3 docs'
print('ALL DOMINO CHECKS PASSED')
"</automated>
  </verify>

  <acceptance_criteria>
    - notebooks/03_domino.ipynb exists with executed output cells
    - communes_master.parquet contains new columns: region, surface_ha, densite_hab_km2, manques, pct_55plus_dept, medecins_55plus_est, domino_alert, projection_2030
    - df['region'].notna().sum() > 34000
    - df['densite_hab_km2'].notna().sum() > 34000
    - df['pct_55plus_dept'].notna().sum() > 4000
    - Communes with nb_medecins_total <= 3 have domino_alert = None
    - manques column contains lists (or None) for each commune
  </acceptance_criteria>

  <done>Domino projection computed for 4900+ eligible communes, missing specialties detected, region+density added for twin matching, parquet updated</done>
</task>

</tasks>

<verification>
After both tasks, verify the parquet has all expected columns:
```bash
python3 -c "
import pandas as pd
df = pd.read_parquet('notebooks/data/processed/communes_master.parquet')
expected = ['score','classe','n_score_components','score_apl_norm','score_pauvrete_norm','score_pct75_norm','score_urgences_norm','region','surface_ha','densite_hab_km2','manques','pct_55plus_dept','medecins_55plus_est','domino_alert','projection_2030']
missing = [c for c in expected if c not in df.columns]
assert not missing, f'Missing columns: {missing}'
print(f'Parquet: {df.shape[0]} rows, {df.shape[1]} columns — all expected columns present')
print(f'Score coverage: {df[\"score\"].notna().mean():.1%}')
print(f'Domino eligible: {(df[\"nb_medecins_total\"] > 3).sum()}')
"
```
</verification>

<success_criteria>
- 02_score.ipynb executed: score 0-10 for >34000 communes, A-E classification, national medians JSON
- 03_domino.ipynb executed: domino alert for eligible communes, missing specialties, region+density columns
- communes_master.parquet has ~44 columns (29 original + ~15 new)
- All inline assertions pass in both notebooks
</success_criteria>

<output>
After completion, create `.planning/phases/02-scoring-clustering/02-01-SUMMARY.md`
</output>
